# Draculas Fortune

A simple fortune script that outputs a random line from the draculas-fortune list.

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/Keiyoko/draculas-fortune/main/install.sh | bash
```

Then restart your terminal or run:

```bash
source ~/.bashrc
```

## Usage

```
flow
```
